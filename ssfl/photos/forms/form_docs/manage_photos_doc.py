# Documentation fields for form upload_photos
docs = dict()

entry = dict()

# Fields used in all choices
docs['ph_up'] = entry
x = """The directory into which the photos will be placed.  (This should be removed and be behind the scenes).
"""
entry['directory'] = [x]


